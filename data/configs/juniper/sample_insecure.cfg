system {
    services {
        telnet;
        ssh {
            protocol-version v1;
        }
    }

    login {
        retry-options {
            tries-before-disconnect 10;
        }
    }

    syslog {
        user * {
            any emergency;
        }
    }
}